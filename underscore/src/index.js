import contains from '@js/contains'
import extendOwn from '@js/extendOwn'
import assign from '@js/assign'
import findIndex from '@js/findIndex'
import identity from '@js/identity'
import include from '@js/include'
import includes from '@js/includes'
import indexOf from '@js/indexOf'
import {
  isArguments,
  isFunction,
  isString,
  isNumber,
  isDate,
  isRegExp,
  isError,
  isSymbol,
  isMap,
  isWeakMap,
  isSet,
  isWeakSet
} from '@js/isAny'
import isArray from '@js/isArray'
import isMatch from '@js/isMatch'
import isNaN from '@js/isNaN'
import isObject from '@js/isObject'
import iteratee from '@js/iteratee'
import keys from '@js/keys'
import matcher from '@js/matcher'
import matches from '@js/matches'
import property from '@js/property'
import sortedIndex from '@js/sortedIndex'
import values from '@js/values'
import each from '@js/each'
import forEach from '@js/forEach'
const VERSION = '1.0.0'

export {
  isArguments,
  isFunction,
  isString,
  isNumber,
  isDate,
  isRegExp,
  isError,
  isSymbol,
  isMap,
  isWeakMap,
  isSet,
  isWeakSet,
  VERSION,
  iteratee,
  isObject,
  identity,
  contains,
  includes,
  include,
  assign,
  extendOwn,
  findIndex,
  indexOf,
  isNaN,
  keys,
  matcher,
  matches,
  property,
  sortedIndex,
  values,
  isArray,
  isMatch,
  each,
  forEach
}